"""cnct_dataprep — standalone TIGRE-based forward projection and FDK preprocessing.

This package is fully decoupled from the cnct PyTorch training pipeline. It
produces the on-disk artifacts (projections.npy, recon_fdk.npy) that the cnct
package consumes as inputs.
"""

__version__ = "0.1.0"
