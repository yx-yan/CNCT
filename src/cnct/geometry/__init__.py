"""CT geometry construction and HU↔mu conversions used by the DBP layer.

Note:
    This module is intentionally duplicated from ``cnct_dataprep.geometry``.
    The two packages are fully decoupled by design — see the refactor plan.
"""
